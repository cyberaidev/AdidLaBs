[pytest]
# Run from backend/ so `common`, `session`, etc. import as top-level modules
# exactly as they do in the Lambda package.
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
